#!/bin/bash
# $3 is 0 for file checkout, 1 for branch checkout
if [ "$3" = "0" ]; then exit 0; fi
if git submodule status | grep -q '^[-+]'; then
    echo "submodules out of sync — running: mise run update"
    mise run update
fi
